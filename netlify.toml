[build]
  command = "npm run build"
  publish = "dist"

# Optional: cache node_modules for faster builds
[build.environment]
  NPM_FLAGS = "--legacy-peer-deps"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200